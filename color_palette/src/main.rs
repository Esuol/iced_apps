use iced::alignment::{self, Alignment};
use iced::mouse;
use iced::widget::canvas::{self, Canvas, Frame, Geometry, Path};
use iced::widget::{column, row, text, Slider};
use iced::{
    Color, Element, Font, Length, Pixels, Point, Rectangle, Renderer, Size, Vector,
}
use plaette::{convert::FromColor, rgb::Rgb, Darken, Hsl, Lighten, ShiftHue, Saturate, Shade};

fn main() {
    println!("Hello, world!");
}
